# ---- Output di build .NET (rigenerati con `dotnet build`) ----
src/StreamingDoc.App/bin/
src/StreamingDoc.App/obj/

# ---- Output di publish / single-file (rigenerati; alcuni >100MB) ----
publish/
publish_single/

# ---- Installer compilato (rigenerato con ISCC; lo script .iss resta versionato) ----
installer/*.exe

# ---- Obfuscar (mapping generato ad ogni run) ----
obfuscar-mapping.txt
publish/_obf/

# ---- IDE / vari ----
.vs/
*.user
*.suo
createdump.exe

# NOTA: third_party/ffmpeg/ e' VOLUTAMENTE incluso (DLL native FFmpeg, non su NuGet).
