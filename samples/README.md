# File di esempio (playout SCTE-35)

- `scte_default.xml` — palinsesto XMLTV di esempio con stacchi pubblicitari (ADNPLAY/PROMO).
- `scte_trigger.xml` — trigger minimale immediato (per test: fira uno splice al volo).

In Impostazioni → Stream → "File trigger del playout" indica il percorso di uno di questi.
