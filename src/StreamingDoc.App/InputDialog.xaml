<Window x:Class="StreamingDoc.App.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="170" Width="420" Icon="NexusStreamer.ico" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" Background="{StaticResource B.Window}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" Grid.Row="0" Text="" Margin="0,0,0,8" TextWrapping="Wrap"/>
        <TextBox x:Name="Input" Grid.Row="1" Padding="6,5"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{Binding [Cancel], Source={StaticResource Loc}}" Width="96" Click="OnCancel"/>
            <Button Content="{Binding [OK], Source={StaticResource Loc}}" Width="96" Style="{StaticResource AccentButton}" Click="OnOk" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
